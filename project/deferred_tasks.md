# Deferred Tasks & Known Issues

Scratch list of things we don't want to forget but aren't worth formalizing into a plan during active exploration and incremental development. Append freely; prune when resolved.

**Related structured plan:** `rzenith_curation_and_bel_plan.md` — phased work plan for rzenith curator evolution, BEL authoring, GO-CAM view, paper-processor subagent. Items in that plan are not duplicated here; this file is for everything else.

---

## Dormant agents — fix before reactivating

As of 2026-04-14, only **rgiskard** and **rzenith** are active. The following three agents exist in `memento/agents/` and have scheduled-task scaffolds in `~/.claude/scheduled-tasks/`, but are dormant and have rot that must be cleaned up before any attempt to schedule or run them.

### drh, rdaneel, janetexample

**Status:** dormant since ~March 19, 2026. SKILL.md path references to `ndexbio/agents/` were patched to `memento/agents/` on 2026-04-14, but the files still have other issues.

**Known issues to fix before reactivation:**

1. **`config.yaml` references are dead** — each SKILL.md has a step reading `memento/agents/<agent>/config.yaml`, but those files don't exist in the memento repo (only `CLAUDE.md` is present). Either create the config.yaml files or remove those steps.

2. **`self_knowledge_specs.md` reference is dead** (drh-session only) — SKILL.md references `ndexbio/demo_staging/self_knowledge_specs.md` which no longer exists anywhere.

3. **Legacy protocol duplication** — the SKILL.md files inline session-lifecycle steps that are now canonically in `memento/agents/SHARED.md`. The rzenith and rgiskard SKILL.md files use a clean delegation pattern ("read CLAUDE.md + SHARED.md, follow the lifecycle there"). drh/rdaneel/janetexample should be rewritten to match that template.

4. **Profile choice decision** — rzenith and rgiskard scheduled tasks use `local-<agent>` profiles (local NDEx server, 127.0.0.1:8080). The legacy SKILL.md files assume the public NDEx (`profile="<agent>"`). Decide which server each agent targets before reactivating.

5. **No self-knowledge networks on local NDEx** — if these agents are reactivated against the local server, they'll need to bootstrap `<agent>-plans`, `<agent>-session-history`, `<agent>-collaborator-map`, and `<agent>-papers-read` networks first (rzenith and rgiskard both went through this process manually).

### rgiskard

**Status (2026-04-14):** SKILL.md scaffold created at `~/.claude/scheduled-tasks/rgiskard-session/SKILL.md`, modeled on rzenith's. **Not yet registered as a recurring scheduled task.** When ready to enable, register via `mcp__scheduled-tasks__create_scheduled_task` — suggest ~08:37 PT daily (offset from rzenith's ~09:19 PT so they don't compete for local NDEx or tool permissions).

---

## Infrastructure bugs / friction points

(Surfaced during 2026-04-14 verification. Not yet fixed.)

- **Scheduled-task PubMed/WebFetch calls blocked by permission prompt** — today's rzenith run died at `mcp__pubmed__search_pubmed` with "Tool permission stream closed before response received". Need to figure out the allowlist / `settings.json` config so scheduled agents can use external MCP tools autonomously. This currently blocks any research-heavy scheduled session. (Discussed in 2026-04-14 session; TBD.)

- **Session finalization on crash** — when a scheduled session aborts mid-flight, no `session-N` node gets written to session-history. Agent protocols currently finalize only at clean end. Consider: write a partial `session-N` node early and update at end, or wrap work in try/finally pattern so crashes still leave a trace.

- **`set_network_visibility` MCP flow** — creating a network on local NDEx still requires a follow-up `PUT /v2/network/{uuid}/systemproperty` to set visibility=PUBLIC. Known issue; no root-cause fix yet.

- **Boolean coercion bug** — `False → "False" → truthy` on MAP property round-trip through LadybugDB.

- **`_wait_for_valid` silent failure** — after 30s timeout, returns without raising. Should raise.

- **CX1 vs CX2 format inconsistency** — some network reads return CX2 (dict), others CX1 (array of aspect blocks). `local_store/cx2_import.py` normalizes this; external consumers still have to branch.

- **ToolSearch round-trip per scheduled run** — each scheduled session has to call `ToolSearch` to load deferred `mcp__local_store__session_init` schema before calling it. Minor latency cost, not blocking.

- **Local store single-writer lock (same-agent concurrency)** — LadybugDB takes an exclusive lock on `~/.ndex/cache/<agent>/graph.db` at open time. This blocks EVEN READS from a second client, so two processes touching the same agent's cache will collide (observed 2026-04-14 with rzenith in a parallel session). Cross-agent (rzenith vs rgiskard) is fine because each has its own cache dir. Same-agent collisions will hit when: (a) scheduled rzenith is running and the interactive session wants to inspect rzenith's state, (b) two interactive sessions both use `store_agent="rzenith"`, (c) overlapping scheduled runs if cron jitter aligns. **Mitigated 2026-04-17 via per-agent `--agent-scope` MCP server entries** (see `memento/project/architecture/local_store_lock_isolation.md`): each scheduled task now uses its own scoped `local_store_<agent>` entry that rejects calls for other agents before opening the database, preventing cross-session lock contention. Interactive multi-agent queries via the unscoped `local_store` entry can still collide with running scheduled tasks — long-term fix (Option B open/query/close, or WAL mode) remains future work.

- **WAL recovery blocks current MCP after concurrent-writer kill** — when a second process holding the lock is killed (rather than cleanly closing), it leaves a `graph.db.wal` file. The surviving MCP server's persistent connection then cannot recover or flush this WAL, and subsequent ops (including reads) fail with the same lock error even when only the surviving MCP holds a handle. Workaround: restart Claude so the MCP server opens fresh. Proper fix: MCP client should detect the condition and reopen, or LadybugDB should be configured to auto-recover WAL on subsequent open. Surfaced 2026-04-14 during the A2 backfill smoke test. **Mitigated 2026-04-19** via two changes in `memento/tools/local_store/`: (a) `graph_store._open_database_with_wal_recovery` catches LadybugDB's "Corrupted wal" error on open, deletes the stale `.wal` sidecar, and retries once — safe because NDEx is the canonical source and `session_init` re-imports from it; (b) `server.py` now registers `atexit` + SIGTERM handlers that run `CHECKPOINT` + `store.close()` on graceful shutdown, reducing how often a stale WAL is produced in the first place. SIGKILL / crash still produces a torn WAL, but (a) makes the next open self-healing. Remaining gap: if an orphan process holds the file lock, the retry blocks on lock acquisition rather than a WAL error — see next item.

- **Orphan `local_store.server` subprocesses accumulate across Claude Code sessions** — observed 2026-04-19 with 23 live `python -m tools.local_store.server` processes spanning ~11 hours, one per session/reconnect, never reaped on session end. Each orphan holds an exclusive lock on whichever agent caches it opened, blocking any fresh process (including the newly-spawned MCP server for the next session) from opening the same cache. This is the root cause of the "Could not set lock on file" errors that surface *after* WAL recovery succeeds. Two coupled problems: (1) Claude Code's stdio MCP child lifecycle — subprocesses aren't SIGTERM'd on session end; (2) when they are eventually killed (reboot, manual `kill`), the kill is abrupt and produces the torn-WAL pattern fixed above. Temporary workaround: fully quit the Claude desktop app (Cmd+Q) to reap the current session's child; older orphans die on reboot. Proper fix options: (a) investigate the desktop-app / Claude Code session-end hook to ensure child SIGTERM; (b) add a heartbeat in `server.py` that detects a dead MCP client (stdin EOF) and self-terminates cleanly. Surfaced 2026-04-19 during rdaneel WAL-recovery verification.

- **PubMed MCP PMID→PMCID mismapping defect** — `get_pubmed_abstract(pmid)` returns a `pmcid` / `doi` pair that does not always correspond to the requested PMID. Confirmed 2026-04-14 on PMID 38200309 (Cho 2024 *Nature*, MRE11→cGAS), where the tool returned PMCID PMC7515726 / DOI 10.1093/nar/gkaa544 — those map to Skrajna 2020 *NAR* ("Comprehensive nucleosome interactome screen..."), an apparently *cited* paper, not the requested one. Using that PMCID with `get_pmc_fulltext` silently yields the wrong paper. **Operational mitigation in place**: the paper-processor subagent (`memento/workflows/BEL/subagent/SUBAGENT.md` §3) verifies PMC title against the PubMed title, and recovers via `search_pmc_fulltext(<title>)` when they mismatch. Any other caller that reads PMC full text via a PMID should do the same check. **Root-cause fix**: investigate `tools/pubmed/` — suspect an NCBI ELink traversal bug or an accidental "cites" link being followed. Surfaced 2026-04-14 during Phase B B2 smoke test.

- **`session_init` fetches procedures as fifth self-knowledge network — DONE 2026-04-19** in `memento/tools/local_store/server.py`. SHARED.md §Self-Knowledge Networks was updated 2026-04-18 to canonicalize `<agent>-procedures` as the fifth standard network; session_init now includes it in `sk_types` and `key_map`. Backward-compatible: agents without a procedures network yet have it silently skipped (search returns numFound=0 → key never added to uuids dict → no error). Corresponding doc updates landed in `memento/CLAUDE.md` and `memento/agents/SHARED.md`. Rdaneel plan item was the driver.

- **`session_init` `active_plans` filter is over-strict** — server.py:802 filters actions by `properties.status == "active"`, but the controlled vocabulary for action status is {active, planned, done, blocked} and agents use `planned` for not-yet-started items. Result: `active_plans` is routinely empty even when there's a real queue. Confirmed 2026-04-19 on rdaneel — `active_plans: []` returned while the plans network held 10 `status: "planned"` actions. Proposed fix: broaden the filter to `status in {"active", "planned"}` (or accept a status-whitelist parameter). Low-risk single-line change in `tools/local_store/server.py`; may want to rename the returned key to `pending_plans` for clarity if the semantics shift. Surfaced 2026-04-19 during rdaneel session-init diagnosis.

- **argparse-at-import breaks pytest collection of `test_mcp_tools.py`** — `tools/local_store/server.py` calls `parser.parse_args()` at module import time (lines 38-42). Pytest's argv (e.g. `pytest tests/local_store/`) gets consumed by the local parser, which doesn't know about any of pytest's flags or positional args, and calls `sys.exit(2)`. Result: any test that imports the server module (currently only `test_mcp_tools.py`) fails at collection with `INTERNALERROR SystemExit: 2`. Tests that import only `store`, `catalog`, `graph_store` (tiers 0-7) run fine. Fix: change `parser.parse_args()` → `parser.parse_known_args()[0]` in `_parse_args()` — argparse will ignore pytest's unknown flags/positionals. Introduced 2026-04-17 with the `--agent-scope` work; unnoticed because the passing tiered tests gave false confidence. Surfaced 2026-04-19 during session_init procedures-fifth-network verification.

- **BEL representation of second-messenger specificity** — BEL statements like `a(CHEBI:"2',3'-cGAMP") directlyIncreases act(p(HGNC:STING1))` are technically true but oversell causality. Second messengers (cGAMP, cAMP, cGMP, Ca²⁺, IP3, DAG) are broadly diffusible; specificity comes from spatial compartmentalization (AKAPs, microdomains, scaffolds), local phosphodiesterase activity, transport (e.g., cGAMP via SLC19A1, LRRC8, gap junctions), pulsatile dynamics, and effector affinity thresholds. Upregulating the messenger does not uniformly activate every downstream effector it *could* bind. The cGAMP-specific literature on localization/transport is still evolving. Open questions: (a) should every second-messenger edge carry a mandatory `context` or `compartment` annotation? (b) can BEL's `loc()` qualifier express "intracellular cGAMP at ER" vs "extracellular cGAMP"? — partial yes, doesn't capture temporal dynamics; (c) are there emerging GO-CAM or BEL conventions for this case? Concrete action: once curation pattern accumulates, write a `second-messenger-authoring.md` note in `memento/workflows/BEL/reference/` with the standing conventions. Until then, every second-messenger edge gets a scope annotation flagging localization/transport caveats. Surfaced 2026-04-14 on cGAS→cGAMP→STING edge during A2 backfill.

---

## Potential tasks (not yet started)

- **Phase 4 analytics pipeline** — cross-agent consultation metrics on local NDEx (15+ networks, 2 active agents, 1 confirmed thread).
- **rgiskard Session 4 (manual or scheduled)** — integrate rzenith's response + KB v1.1 into mechanism map V2.
- **agent-hub-dev verification** — confirm the consultation thread renders correctly in the browser webapp.
- **Docker snapshot** — freeze current local NDEx state for reproducibility.
- **Partial cache reload** — skip re-download in `session_init` if NDEx mod time unchanged.
- **Promote common properties to indexed columns** (status, priority, evidence_tier) in local_store graph schema to avoid Python post-filtering.
- **Memory UUID refresh** — prior session memory doc lists stale UUIDs from pre-restart state (e.g. rzenith expert response `5f526030-369f-*` vs actual `3b07a1ba-36f1-*`). Update or annotate when convenient.

---

## Log

- 2026-04-14: file created during infrastructure verification session.
- 2026-04-14: global allow list expanded in `~/.claude/settings.json` to add `mcp__pubmed__*` and `mcp__local_store__*` — unblocks the PubMed permission crash that killed today's scheduled rzenith run.
- 2026-04-14: rgiskard scheduled-task SKILL.md scaffold created (not yet cron-registered); drh/rdaneel/janetexample SKILL.md files had `ndexbio/agents/` → `memento/agents/` path patches but still carry other rot documented above.
- 2026-04-14: BEL SKILL.md drafted at `memento/workflows/BEL/SKILL.md`. Structured plan for rzenith curator evolution + BEL integration created at `ndexbio/project/rzenith_curation_and_bel_plan.md`.
- 2026-04-14: Phase A backfill dry-run completed (3 edges); 5 protocol amendments applied to SHARED.md, rzenith/CLAUDE.md, and review_log_network.md. Two deferred infra issues logged (WAL recovery, second-messenger representation). Phase B (paper-processor subagent) handed off to a fresh Claude session for context preservation.
- 2026-04-19: WAL-recovery mitigation landed in `memento/tools/local_store/` (graph_store self-heal on open + server.py atexit/SIGTERM checkpoint). Confirmed working against rdaneel's corrupt WAL during rdaneel session-init diagnosis. Verification surfaced a second problem — 23 orphan `local_store.server` subprocesses accumulating across Claude Code sessions — logged as a separate item. Root cause of today's rdaneel wedge traced end-to-end: LadybugDB = Kuzu (renamed); Kuzu does not checkpoint on Database destruction (upstream issue #4013); when an orphan MCP subprocess is killed mid-write the resulting torn WAL is not auto-recoverable on next open.
- 2026-04-19: `session_init` extended to fetch `<agent>-procedures` as the fifth self-knowledge network (closes rdaneel plan item; aligns tool with SHARED.md §Self-Knowledge Networks). Updates to `memento/CLAUDE.md` and `memento/agents/SHARED.md` counted from "four" to "five" and added the procedures row + silent-skip note. Two pre-existing bugs surfaced during verification and logged above: over-strict `active_plans` status filter; argparse-at-import breaking pytest `test_mcp_tools.py`.
- 2026-04-20: Paper-fetching pre-phase work landed ahead of the planned dexter-as-human-courier protocol (Phase 1). (a) Audit confirmed `biorxiv::get_paper_fulltext` already falls back to Europe PMC in code (known-issues doc previously made this sound like an unimplemented recommendation; corrected in `memento/CLAUDE.md`). (b) New MCP tool `pubmed::find_free_fulltext(doi)` backed by `tools/pubmed/unpaywall.py` — Unpaywall returns all known free-fulltext URLs (author-deposited preprints, institutional repositories, publisher OA copies) for a DOI. Agents hitting a PMC miss should call this BEFORE escalating to a human paper-request; escalate only when `is_oa=False` with empty locations. 16 unit + live-API tests pass. Verified end-to-end on (i) Nature 2013 paper → 4 free locations incl. arXiv + Harvard IR, (ii) bioRxiv preprint → bioRxiv + PMC copies, (iii) unknown DOI → clean error. This implements rdaneel plan item equivalent "preprint-finder tool" that was not yet on the plans network. Claim-escalation discipline for agents deferred per user preference (err toward emergent discovery rather than rationing fulltext requests).
- 2026-04-20 (Phase 1 — dexter as human utility participant): Landed the infrastructure for scientist agents to escalate paywalled paper fetches to the human operator. (a) Created `dexter` NDEx account on local test server (UUID `1bcb236f-3d0e-11f1-84e0-02b2a9d51b89`); password `Dexter#Courier2026` stored in `~/.ndex/config.json` under `local-dexter` profile — **change this via NDEx UI when convenient**. Pre-existing `dexter` profile pointing at public NDEx (password `BELridesagain#70`) was left untouched — different server, unrelated. (b) Workspace dir at `~/.ndex/cache/dexter/scratch/` created (for consistency with other participants; dexter's actual fetches happen on UCSD machine outside this directory). (c) `ndex-message-type` taxonomy in `ndexbio/project/architecture/agent_communication_design.md` extended with `paper-request` and `paper-fulfilled`. Full protocol documented including dedup rules (search-by-DOI before creating), required properties (`ndex-target-agent: dexter`, `ndex-doi`, `paper-title`, `requesting_agent`, `reason`, `priority`, `unpaywall_checked`), fulfillment thread structure (`ndex-reply-to` + `disposition` ∈ {fulfilled, unavailable, deferred}), and extraction-tier levels (1=claims, 2=excerpts, 3=verbatim; default 1). (d) `memento/agents/SHARED.md` gained a "Paper Access Protocol" section instructing agents on the fallback order — PMC → bioRxiv → Unpaywall → dexter — and the exact publish/dedupe/poll flow. Agents check for fulfillments via `search_networks(query="ndex-message-type:paper-fulfilled ndex-reply-to:<request-uuid>")` at session start. Phase 2 (agent-side helper tool/procedure) and Phase 3 (agent-hub-dev courier view) remain future work. No test request has been authored yet; rsolar's next scheduled session will exercise the path naturally when it hits an Unpaywall miss.
- 2026-04-20 (afternoon batch): Follow-up discovery after the pre-afternoon /tmp fixes. rzenith's afternoon scheduled session correctly picked up the new Workspace-directory discipline and wrote to `~/.ndex/cache/rzenith/scratch/kb_v1.7_spec.json` — but still hit a "Path is outside allowed working directories" permission prompt. Root cause: **two distinct permission layers**, and morning's fix only addressed one. (1) Python MCP subprocess file I/O (e.g. `ndex_mcp.download_network`) bypasses Claude Code's permission prompts entirely because it's direct filesystem access — my morning fix (tempfile default → `~/.ndex/scratch/`) correctly addressed this layer. (2) Claude Code's Write tool checks `permissions.additionalDirectories` in `settings.local.json` before allowing writes — this is a separate allowlist that the Python-side fix doesn't touch. `~/.ndex/` was not in the project's allowlist, so every Write-tool call to the new correct path still prompted. **Fix**: added `~/.ndex` to `permissions.additionalDirectories` in `~/Documents/agents/.claude/settings.local.json` (162 existing allow entries preserved). Future agent sessions will Write-tool-call cleanly to their per-agent scratch dirs. Currently-running sessions loaded their permission snapshot before the settings edit and still need manual "Allow once" on any pending dialog. Good-news verification: rzenith's afternoon session independently adopted the new workspace path after reading the updated CLAUDE.md — the agent-doc discipline works; only the settings-allowlist needed the extra step. rcorona's afternoon session completed cleanly after morning fixes landed. rdaneel still fails scheduled-review with orphan-lock (unchanged, separate deferred item).
- 2026-04-20 (pre-afternoon-batch): /tmp-write hang diagnosed and fixed systemically. rzenith and rcorona both hung on Write-tool permission prompts ("Path is outside allowed working directories — /tmp/rzenith_kb_v1.6.json"). **Two compounding bugs**: (1) `ndex_mcp.server.download_network` defaulted to `tempfile.mkdtemp(prefix="ndex_")` when `output_dir` wasn't passed → creates `/tmp/ndex_*/` dirs that scheduled-task sandboxes block on write; (2) only 2 of 11 agent CLAUDE.md files (rgiskard, rsentinel) had the "Workspace directory" discipline section — the other 9 had no guidance on where to put transient files. **Fixes applied**: (a) `ndex_mcp/server.py` default changed to `~/.ndex/scratch/` (guaranteed writable, not /tmp); `tempfile` import removed; `output_dir` now expanduser-normalized. (b) "Workspace directory" section added to rzenith, rcorona, rsolar, rsolstice, rvernal, rboreal, rdaneel CLAUDE.md files with `~/.ndex/cache/<agent>/scratch/` path and explicit /tmp prohibition. (c) SHARED.md Unattended Session Protocol gained a "Never write to /tmp from any tool" rule companion to the existing "Never read /tmp" rule. (d) `~/.ndex/cache/<agent>/scratch/` dirs created for all 9 agents plus `~/.ndex/scratch/` shared dir. rzenith's and rcorona's currently-running sessions still need "Allow once" from the human on the pending dialog (they loaded their prompts before this fix); afternoon scheduled runs will spawn fresh MCP subprocesses with the corrected defaults. drh and janetexample CLAUDE.md files NOT updated — dormant per earlier `drh, rdaneel, janetexample` note; to be addressed if/when reactivated.
